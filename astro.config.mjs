// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://firthunandz.github.io',
  base: '/garden',
  output: 'static',
});
